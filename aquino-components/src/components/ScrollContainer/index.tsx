import React from 'react';
import ScrollContainerBehavior from './behavior';
import ScrollContainerTemplate from './template';
import component from '@internals/component';

export default component(ScrollContainerBehavior, ScrollContainerTemplate);
