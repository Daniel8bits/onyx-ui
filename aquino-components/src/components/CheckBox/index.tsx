import React from 'react';
import CheckBoxBehavior from './behavior';
import CheckBoxTemplate from './template';
import component from '@internals/component';

export default component(CheckBoxBehavior, CheckBoxTemplate);
