import React from 'react';
import TextfieldBehavior from './behavior';
import TextfieldTemplate from './template';
import component from '@internals/component';

export default component(TextfieldBehavior, TextfieldTemplate);
