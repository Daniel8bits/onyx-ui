import React from 'react';
import NumericTextfieldBehavior from './behavior';
import TextfieldTemplate from '../Textfield/template';
import component from '@internals/component';

export default component(NumericTextfieldBehavior, TextfieldTemplate);
