import React from 'react';
import ComboBoxBehavior from './behavior';
import ComboBoxTemplate from './template';
import component from '@internals/component';

export default component(ComboBoxBehavior, ComboBoxTemplate);
