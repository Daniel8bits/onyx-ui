import React from 'react';
import TableBehavior from './behavior';
import TableTemplate from './template';
import component from '@internals/component';

export default component(TableBehavior, TableTemplate);
