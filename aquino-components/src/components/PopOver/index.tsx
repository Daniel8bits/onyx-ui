import React from 'react';
import PopOverTemplate from './template';
import PopOverBehavior from './behavior';
import component from '@internals/component';

export default component(PopOverBehavior, PopOverTemplate);
