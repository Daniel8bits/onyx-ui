import React from 'react';
import DatePickerTemplate from './template';
import DatePickerBehavior from './behavior';
import component from '@internals/component';

export default component(DatePickerBehavior, DatePickerTemplate);
