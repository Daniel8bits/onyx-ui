import React from 'react';
import DatePickerWeeksBehavior from './behavior';
import DatePickerWeeksTemplate from './template';
import component from '@internals/component';

export default component(DatePickerWeeksBehavior, DatePickerWeeksTemplate);
