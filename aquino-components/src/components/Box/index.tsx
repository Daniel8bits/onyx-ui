import React from 'react';
import BoxBehavior from './behavior';
import BoxTemplate from './template';
import component from '@internals/component';

export default component(BoxBehavior, BoxTemplate);
