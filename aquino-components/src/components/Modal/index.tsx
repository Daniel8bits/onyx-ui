import React from 'react';
import ModalTemplate from './template';
import ModalBehavior from './behavior';
import component from '@internals/component';

export default component(ModalBehavior, ModalTemplate);
